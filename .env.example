# Copy to .env to override (not committed -- see .gitignore).

# TAP device name and host-side CIDR that `make setup-tap` /
# `make bootstrap` (scripts/make_tap.sh) bring up.
CELLA_TAP=tap0
CELLA_TAP_CIDR=192.168.200.1/24
