# The gateway kernel: the canonical fragment plus what transparent
# termination needs -- netfilter with TPROXY, and policy routing, so
# the proxy can accept a connection addressed to any destination
# (I
